import React from 'react'
import Profile from "@/app/[locale]/profile/Profile";

type Props = {}

const page = (props: Props) => {
    return (
        <div><Profile/></div>
    )
}

export default page